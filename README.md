# smusher
Image minification task to run on a directory and process images

### `$ grunt`
Runs `imagemin` on all files and directories within `/input` and re-runs this task when new files are added. 

### `$ grunt reset`
Empties the `/input` directory for all files and directories.
